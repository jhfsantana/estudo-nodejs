<!DOCTYPE html>
<html>
<head>
	<title>Formulario para cadastro de produtos</title>
</head>
<body>
<form action="/produtos" method="POST">
	<ul>
		<% for(var i=0; i < errors.length; i++) { %>
			<li><%= errors[i].param %> - <%= errors[i].msg %></li>
		<% } %>
	</ul>
	<div>
		<label for="titulo">Titulo</label>
		<input type="text" name="titulo" id="titulo" value="<%= produto.titulo%>" />
	</div>
	<div>
		<label for="preco">Preco</label>
		<input type="text" name="preco" id="preco" value="<%= produto.preco%>" />
	</div>
	<div>
		<label for="descricao">Descricao</label>
		<textarea id="descricao" name="descricao" rows="10" cols="20"><%= produto.descricao%></textarea>
	<input type="submit" value="Salvar">
</form>
</body>
</html>